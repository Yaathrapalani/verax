# Selective Prediction Specification

## Overview
Selective prediction defines the explicit `ACCEPT` or `REJECT` option for model outputs.
Predictions failing any reliability check or exceeding uncertainty bounds are rejected from consequential decision-making.
