# Scenario Comparison Specification

## Protocol
Calculates baseline value, scenario value, absolute delta (\(Y - X\)), and relative delta (\((Y - X)/X\)) for every target engineering variable.
If baseline value is zero or unavailable, `relative_delta` is set to `None` / `UNAVAILABLE`.
