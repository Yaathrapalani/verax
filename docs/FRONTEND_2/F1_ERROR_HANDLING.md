# F1 Error Handling Report

Documents handling of `LOADING`, `ERROR`, `UNAVAILABLE`, `EMPTY`, and `SUCCESS` states. When the backend fails, an explicit `UNAVAILABLE` or error panel is displayed instead of silently falling back to un-indicated mock data.
