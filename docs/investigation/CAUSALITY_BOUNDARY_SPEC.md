# Causality Boundary Specification

## Boundary Rules
1. Stage 8 is an evidence-driven investigation engine, NOT a root-cause oracle.
2. Aggregate \(R_f\) signal alone cannot establish a unique chemical fouling mechanism.
3. Output claims must never exceed evidence (e.g. `mechanism_claim = "NONE"`).
