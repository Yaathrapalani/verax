# Discriminating Observation Specification

## Purpose
Identifies observations that best discriminate between competing candidate hypotheses (e.g., \(\Delta P\) history helps discriminate flow/hydraulic effects from thermal boundary layer fouling). Information gain basis is explicitly tagged as `HEURISTIC`.
