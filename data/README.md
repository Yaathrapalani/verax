# Data Directory

`raw/` = source files exactly as obtained.
`interim/` = temporary transformations.
`processed/` = validated/model-ready datasets.

Every dataset must have provenance recorded in its accompanying metadata. Never label synthetic or simulated data as real industrial data.
